# Python
venv/
__pycache__/
*.pyc

# Salidas generadas (se regeneran con `python main.py`)
informe.html
resultados.json

# Preview local (solo la copia en la raíz; docs/index.html SÍ se publica en Pages)
/index.html
.claude/
